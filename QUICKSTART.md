# Быстрый старт 🚀

## Запуск симулятора
```bash
cargo run --release
```

## Управление

| Клавиша | Действие |
|---------|----------|
| **Tab** | Переключить камеру (вид сверху ↔ от 3-го лица) |
| **↑↓←→** | Движение камеры (вид сверху) |
| **+/-** | Приближение/отдаление |
| **ЛКМ** | Выбрать танк |
| **W/S** | Вперед/назад (управление танком) |
| **A/D** | Поворот (управление танком) |
| **Пробел** | Выстрел |

## Что происходит?

1. **Генерация**: Создаются 20 танков со случайными нейронными сетями
2. **Бой**: Танки сражаются 2 минуты (синие vs красные)
3. **Селекция**: Лучшие 20% выживают
4. **Эволюция**: Скрещивание + мутация = новое поколение
5. **Повторить**: Цикл продолжается бесконечно

## Параметры ИИ

- **Входы**: 8 (позиция врага, здоровье, углы, и т.д.)
- **Скрытые**: 8 нейронов
- **Выходы**: 4 (движение, поворот, стрельба)
- **Веса**: 96 параметров на танк

## Фитнес

```
Успешность = Убийства × 100 + Время_жизни × 2
```

## Файлы

- `best_genomes.json` - автосохранение лучших танков
- `evolution_history.json` - история эволюции (если добавить)

## Изменение параметров

### Размер популяции
[main.rs](src/main.rs#L67):
```rust
Population::new(20)  // Измените на нужное число
```

### Время боя
[battle.rs](src/battle.rs#L27):
```rust
max_battle_time: 120.0  // Секунды
```

### Количество танков
[battle.rs](src/battle.rs#L52):
```rust
for i in 0..10  // Измените количество
```

### Параметры мутации
[genetics.rs](src/genetics.rs#L120):
```rust
mutation_rate = 0.1        // Вероятность (0.0-1.0)
mutation_strength = 0.3    // Сила изменения
```

## Добавить управляемый танк

Откройте [battle.rs](src/battle.rs) и в `spawn_initial_tanks` замените одного танка на:

```rust
let player_tank = spawn_tank(
    commands, meshes, materials,
    Vec3::new(0.0, 1.0, 0.0), 0, None
);
commands.entity(player_tank).insert(PlayerControlled);
```

## Отладка

Включите логи:
```bash
$env:RUST_LOG="info"; cargo run --release
```

## Производительность

Если лагает:
1. Уменьшите количество танков
2. Уменьшите качество теней в коде
3. Закройте другие приложения

## Следующие шаги

- Посмотрите [EXAMPLES.md](EXAMPLES.md) для продвинутых примеров
- Читайте [README.md](README.md) для полной документации
- Экспериментируйте с параметрами!

---
**Приятной эволюции! 🎮🤖**
